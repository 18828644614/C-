# Notes: C++ 函数与作用域章节

## 现有文档结构
- 文件：`01-基础入门/函数与作用域.md`。
- 保留的主标题：`# 函数与作用域`、`## 学习目标`、`## 要点`、`## 示例与实践`、`## 关联`。
- 原正文基本为空，需在不改变主标题层级的情况下完整扩写。

## 改写要点
- 函数主线：为什么需要函数 → 声明/定义/调用 → 参数与返回值 → 值传递、引用和指针 → 重载与默认参数 → 递归和设计建议。
- 作用域主线：块作用域、函数参数/局部变量、命名空间作用域、全局变量、类作用域；区分“可见范围”和“生命周期”。
- 解释名称隐藏、`::` 作用域运算符、`static` 局部变量和 `const` 引用等初学者易混淆点。
- 提供单文件、可在 Windows 的 MSVC 或 MinGW-w64 下运行的 C++20 示例，代码附中文注释和 PowerShell 命令。
- 汇总常见编译/运行错误，并用练习帮助巩固。

## 已完成与校验
- 章节正文已扩展至约 560 行，保留原有主标题和 Markdown YAML front matter。
- 覆盖函数用途、声明/定义/调用、返回值、值/引用/指针参数、默认参数、重载、递归、块/参数/命名空间/类/严格函数作用域、生命周期、`static`、全局变量和设计建议。
- 综合示例已在 MinGW-w64 下使用 `g++ -std=c++20 -Wall -Wextra -pedantic` 编译并运行成功。
- 已检查代码围栏成对出现，PowerShell 命令使用 `.` 前缀运行当前目录程序。
