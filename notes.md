# Notes: “标准库概览”章节

## Repository Findings

- 目标文件：`03-STL与泛型编程/标准库概览.md`。
- 原始结构：front matter、`# 标准库概览`、`## 学习目标`、`## 要点`、`## 示例与实践`、`## 关联`。
- 同目录的后续章节目前也是骨架，因此本章需要承担入门导览职责，但不替代容器、迭代器、算法等专章的深入内容。

## Content Checklist

- 标准库与 STL 的关系，以及标准库并非只有容器。
- 头文件、`std` 命名空间、编译标准、实现和链接的基础关系。
- 容器、迭代器、算法、函数对象/Lambda、字符串/视图/范围、工具、时间、文件系统、并发、错误处理等模块的定位。
- “容器保存数据，算法处理区间，迭代器连接二者”的协作模型。
- 值语义、所有权、生命周期、迭代器失效、异常安全、复杂度和线程安全等横向概念。
- Windows 下使用 MSVC、Visual Studio、`cl` 或 CMake 编译的说明。
- 一个完整可运行的综合示例，并提供常见错误对照。

## Verification Notes

- 示例使用 C++17，避免依赖 Windows 专有 API；在 Windows/MSVC、MinGW 或其他标准实现上均可移植。
- 目标文档最终保留原有 front matter、一级标题和四个二级标题，并在其内部补充三级标题、表格、代码和关联。
- 目标文档共 448 行，代码围栏 24 行（12 个代码块，成对平衡）。
- MinGW `g++` 在 `-std=c++17 -Wall -Wextra -pedantic` 下实际编译并运行两个完整示例，编译和运行返回码均为 0。
- 检查了 `std::` 前缀、头文件自包含、`std::filesystem` 的使用方式、文件路径和输出解释。
- 已特别说明：STL 不等同于整个标准库，`reserve` 不改变 `size`，`string_view` 不拥有字符串，`end()` 不可解引用，以及容器修改可能导致迭代器失效。
